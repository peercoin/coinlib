# Coinlib Flutter example

This is a simple example of using `coinlib` directly in a Flutter application.
The `lib/main.dart` entry point loads the library before starting Flutter and
demonstrates key, message-signing, and MuSig functionality.

Run it from this directory with:

```sh
flutter run
```
